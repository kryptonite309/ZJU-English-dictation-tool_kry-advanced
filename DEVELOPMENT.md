# 大英默写器：每日学习与可定制外观

此目录保存从现有应用行为重建的源码。程序继续读取项目根目录中的：

- `data/` 词书和单元 CSV
- `wrong_words.json` 错题本
- `keybindings.json` 自定义指令

默认使用深色背景、白色普通文字、红色错误提示和绿色正确提示；这些学习内容样式现在可以在外观编辑器中调整。

新增功能：

- v1.2.0 新增独立学习统计看板、例句/听写/拼写三题型组合、字符级拼写差异、四模块分别设置的顺序/单元内随机/词书内随机、暂停与跨重启精确恢复、有效学习计时、可选的结束列表前备份，以及带 SHA256 校验、完整备份和失败回滚的 GitHub 自动更新。听写判定后始终显示正确英文和中文；错题转易错本要求已启用的例句、听写、拼写门槛同时满足，单词仍只能手动“斩”入已掌握。
- v1.1.8 在全部单词本管理中加入“最近编辑优先”排序，并显示最后编辑时间；新学、历史列表复习和错题复习窗口底部新增实时进度条，答错不增加进度，复现答对后才计入；新增只读回看的上一页/下一页快捷键，默认 `Shift+Q` 与 `Shift+E`；单词朗读音量改为可拖动的学习音频总音量滑条，预留给后续正确、错误与斩词提示音共用。
- v1.1.7 为单个英文单词的中文释义补充词性前缀，例如 `apple  n. 苹果`；词组不显示词性。一个单词具有多种词性时显示为 `n./v.` 等形式，词库原文已经按释义标注词性时保留原标注。旧学习进度、未完成列表、撤销记录和全部单词本会在读取时补齐词性，不改变原有归属、计数或作答历史。外部 CSV 可通过 `part_of_speech` 或 `pos` 列显式指定词性，显式值优先于内置映射。
- v1.1.6 修复词库多义词被当作多个单词重复出题的问题。相同英文在同一本词书中只作为一个逻辑单词，所有释义会合并，完全相同的例句会去重；若合并后有多条不同例句，每条都会独立出题，全部完成后才算该词的例句题完成，但每个词在每个列表中的同一题型仍只计一次正确或错误。错题本、易错本与已掌握状态按英文共用。升级时会自动合并旧学习列表和词本中的重复项，并保留已有作答，只补做尚未出现的例句。
- v1.1.5 修复圆角按钮在灰色卡片上暴露黑色矩形底框的问题。按钮控件本身现在按圆角区域裁剪，并在绘制时使用所在卡片的真实背景色清除底层，因此不同容器中的按钮边缘保持一致。
- v1.1.4 彻底禁用自绘圆角按钮的 Windows 原生焦点框与默认按钮边框，解决按钮右侧仍出现白色虚线或残缺方框的问题；按钮文字区域同时增加安全内边距，避免文字贴住边缘。
- v1.1.3 优化例句分阶段提示：输入框已有答案时按 Enter 会直接提交并判定；只有输入框为空时才继续显示首字母和中文。统一修复圆角按钮获得焦点时出现的残缺边框，并在帮助窗口加入项目 GitHub 与 QQ 邮箱。
- v1.1.2 将首字母提示扩展到全部例句填空，包括新学、历史列表复习、错题与易错词复习和自由练习。例句题初始只显示空格；默认按 Enter 后显示首字母，再按一次显示中文释义，最后一次才提交答案。首字母提示可全局关闭，提示/提交键也可单独修改；关闭首字母后第一次按键直接显示中文。
- v1.1.1 修复例句填空沿用词条原形判题的问题。例句题现在以当前句 `[[...]]` 中实际隐藏的时态、单复数或短语形态为答案，普通拼写仍使用词条原形；题目回看和答错提示也显示当前例句的真实答案。
- 自动备份不再复制 Git 内部元数据，清理旧备份前会安全解除备份副本中的只读属性，避免只读 `.idx` 文件导致整次自动备份提示失败。

- 错题复习时可选择是否显示首字母。默认连续答对 3 次后移入“易错本”；答错会重置计数。
- “斩”当前词会将它移入“已掌握”，并从后续普通练习中排除。默认快捷键是 `Ctrl+Shift+M`，可在设置界面改动；也可输入 `/master`。
- 设置界面可打开全部词目的单词本管理窗口，手动调整“未归档 / 错题本 / 易错本 / 已掌握”。
- 最近操作的 5 个不同词会显示在设置底部的待定队列，可直接改所属单词本。
- 首次启动时会从原 `wrong_words.json` 迁移错题，并备份原始文件为 `wrong_words.pre-migration.json`。新状态保存在 `notebook_state.json`，同时继续同步旧格式的错题本文件。

每日学习计划：

- 主界面可分别进入新学、历史提取列表复习、错题与易错词复习。三个部分各自维护未完成状态，因此可以分别“继续”或“结束列表”，不会把其他部分的继续入口挤到新学卡片。手动结束时，已完成展示和答题的词保留在当前列表；未完成词返回原来的候选范围，新学词会回到优先抽取队列。同一部分存在未完成列表时，仍不会创建该部分的新列表。
- 新学可按书本设置配额，默认按单元顺序抽取，也可在设置里选择随机。每次提取生成秒级编号的独立文件，位于项目根目录 `study_lists/`；总状态位于 `study_state.json`。
- 新学先逐词展示英文、例句、中文，然后答题。新学、历史列表复习、错题复习与自由练习均可同时启用例句填空、听写和普通拼写，并分别调整题型先后及单词顺序。顺序模式保持书本/单元原序；单元内随机只打乱各单元内部；词书内随机忽略单元。第一次轮次结束后立即复现错题，直到全部正确；复现答对不累计转入易错本的次数。
- 未完成列表再次进入时会根据当前设置重建尚未完成的题目，因此中途退出后更改例句填空、普通拼写等题型设置会立即生效；已经完成的作答记录不重复计算。
- 新学展示阶段可回看已展示词，回看时直接显示英文、例句和释义。答题阶段可逐题回看原题、当时的作答、判定和正确答案；回看只读，不跨越展示和答题两个阶段。记录随学习状态保存。
- 学习日以当地时间凌晨 04:01 切换：04:00:59 仍归前一日。跨日未完成的词回到未提取词池顶端，下一次新学优先抽取；已经产生的错题记录保留。已斩词从所有题目排除。
- 错题到易错本可分别设置例句、听写和拼写的首次正确次数，默认 0 / 0 / 3，三项同时满足后才会移动。易错本不会因答对自动进入已掌握，只能手动斩。可配置复习间隔天数、按到期日筛选、首字母、模糊答案和每词自定义可接受答案。
- 可设置展示、翻页、暂停、斩词、撤销和手动备份快捷键。撤销最多保留 5 步，跨程序重启保留。暂停会遮住题目并停止计时，重新进入同一模块后可恢复当前题、输入内容、提示阶段和累计时间。
- 设置中的“词书管理”支持导入外部 CSV，也可以重命名现有词书。导入文件至少需要 `english` 和 `chinese` 两列，可选 `examples`、`part_of_speech`（或 `pos`）列；导入或重命名前会先创建手动完整备份，词书重命名会同步更新学习状态中的归属和默认配额。
- 自动全量备份默认每 10 分钟一次、最多 6 份；手动备份独立存放、无上限且不会被自动清理。两类备份位于项目根目录 `backups/auto/` 和 `backups/manual/`。恢复前程序会先创建另一份手动安全备份，再在关闭后由恢复助手还原并重启。
- 主窗口右侧只放学习入口与简要进度；点击顶部“设置”可进入分页窗口调整自由练习、每日计划、词本、外观、日历和备份。
- 外观编辑器可以分别设置普通说明、英文单词、英文例句、中文释义、答对、答错及斩词提示的字体、字号、颜色，并自定义三种反馈文案。文案支持 `{word}`、`{answer}`、`{input}` 占位符。自由练习、新学、历史列表复习、错题复习与日历各自拥有纯色或图片背景、显示方式和图片暗化程度。导入图片复制到项目的 `appearance_images/`，配置保存在 `appearance.json`，两者都进入完整备份。
- 学习统计是独立大窗口，支持今日、近 7 天、近 30 天和累计区间，展示三类每日学习、自由练习、总体/首次正确率、复现次数、有效学习时长和趋势。大日历每天直接展示新学、历史列表复习、错题复习和新学列表数；点击日期可查看并打开对应列表，日历背景图仍可独立设置。
- 新学展示与听写使用 Windows 本机英语语音离线朗读。听写只播放英语、不显示答案；空输入时可按提示键显示中文，提交后同时显示正确英文和中文。设置中的“单词朗读”页可调整开关、自动播放、音色、语速、总音量及重播快捷键。没有英语语音时，程序会在开始前提示安装语音或本次临时关闭听写，不会静默跳题。
- 学习页面可显示独立有效计时，可选分钟或 `HH:MM:SS.mmm`。暂停、切换页面、失去焦点和关闭窗口均停止计时；统计看板统一按分钟汇总。
- 程序可在每次启动时检查 GitHub Releases。自动更新只接受名称含“升级包”的 ZIP，并要求同一 Release 提供 `SHA256SUMS.txt`；校验通过、完整备份成功且用户确认后才替换程序。安装失败会尝试回滚，用户数据目录不会被更新包覆盖。

原有自由练习仍保留在主窗口。每日学习是独立流程；原错题本与词本归属继续共用。

重新构建：

```powershell
.\build.ps1
```

默认输出到 `data/_build/main.exe`。

隔离测试：

```powershell
Start-Process .\_build\main.exe -ArgumentList @('--test-notebooks', '<全新测试目录>', '<报告文件>') -Wait
Start-Process .\_build\main.exe -ArgumentList @('--test-study', '<另一全新测试目录>', '<报告文件>') -Wait
Start-Process .\_build\main.exe -ArgumentList @('--audit-parts-of-speech', '<词性审计报告>') -Wait
```

测试命令只在指定的全新测试目录写数据；不要把正式项目目录作为测试目录。部署前先关闭正在运行的旧版 `main.exe`，再将构建产物复制到项目根目录。

`data/parts_of_speech.csv` 是当前 book2、book3 使用的精简词性映射，来源与许可见 `THIRD_PARTY_NOTICES.md`。
