# 更新日志

## v1.1.7

- 为单个英文单词的中文释义增加词性前缀，词组不显示词性。
- 支持从外部 CSV 的 `part_of_speech` 或 `pos` 列读取词性。
- 自动迁移旧学习状态、历史列表、撤销记录和单词本中的词性数据。
- 修复例句填空即时正确提示显示词条原形的问题，提示与回看现在均显示实际答案形式。
- 旧版默认正确提示会自动迁移为实际答案占位符，用户自定义提示保持不变。
- 完整备份会排除开发构建、测试副本和临时报告，避免自动备份异常膨胀。
- 通过单词本、每日学习、例句判定和词性覆盖回归检查。
