# 更新日志

## v1.2.0

- 新增独立学习统计看板，提供今日、近 7 天、近 30 天和累计数据，并保留可跳转列表的大日历。
- 新增听写题型，可与例句填空、普通拼写同时启用，并为四个学习模块分别设置题型先后。
- 新增顺序、单元内随机、词书内随机三种出题方式，四个学习模块可分别配置。
- 答错时显示字符级拼写差异，并从全部可接受答案中选择最接近的一项比较。
- 新增暂停、毛玻璃或自定义图片遮罩、跨重启精确恢复和独立有效学习计时。
- 进度条统一按“已经正确完成的题目 / 当前总题目”计算，答错和复现过程不会虚增进度。
- 手动结束列表前的手动备份改为可选设置，默认开启。
- 错题转入易错本改为同时检查例句、听写、拼写三项正确门槛；复现答对仍不累计。
- 新增启动检查、SHA-256 校验、完整备份、自动安装和失败回滚组成的 GitHub Release 更新流程。
- 修复暂停期间修改题型或顺序后，后续题目不能立即应用新设置的问题。
- 修复速通计时在部分显示缩放下只显示“本模块用时”、毫秒数被裁掉的问题。
- 单词仍然只能通过手动执行“斩”进入已掌握，任何自动答对次数都不会代替该操作。

## v1.1.8

- 全部单词本管理支持按最后编辑时间逆序排列。
- 新学、历史列表复习和错题复习加入正确完成进度条。
- 新增默认 `Shift+Q` / `Shift+E` 的只读回看快捷键。
- 单词朗读音量改为拖动式总音量设置。

## v1.1.7

- 为单个英文单词的中文释义增加词性前缀，词组不显示词性。
- 支持外部 CSV 的 `part_of_speech` 或 `pos` 列。
- 修复例句填空即时正确提示显示词条原形的问题。
- 升级时保留旧学习状态、列表、撤销记录和单词本数据。
